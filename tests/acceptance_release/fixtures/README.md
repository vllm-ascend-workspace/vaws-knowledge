Isolated fixtures for content migration and evidence checks.

They restate shapes from public loop tests and packaged corpus notes. They are
not NPU measurements, Windows evidence, or official feed snapshots.
The `secret.meta.json` token field is a fake marker for skip-private tests.
