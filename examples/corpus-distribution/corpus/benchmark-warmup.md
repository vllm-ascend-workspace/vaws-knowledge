# Benchmark warmup note

The first benchmark run after a service start includes graph compilation.
Discard its latency numbers and report from the second run onward. Observed
on one CPU-only environment; treat as a reporter observation.
