interface:
  display_name: "Curate Knowledge"
  short_description: "Organize plain Markdown notes without extra task steps"
  default_prompt: "Use $curate-knowledge for the requested note cleanup. Reuse available material, consolidate compatible observations, and preserve known conditions, evidence and uncertainty without adding a template or extra report."

policy:
  allow_implicit_invocation: false
